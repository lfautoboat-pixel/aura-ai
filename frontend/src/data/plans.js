// Real plan ids — match the backend catalog (backend/server.py SUBS) already
// used by the rest of the app's paywall. No mock plans here (see rule 2.1).
export const planIds = ["premium_weekly", "premium_annual"];
export const defaultPlanId = "premium_annual";
